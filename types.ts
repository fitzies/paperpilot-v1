type MenuType = {
  Humanize: boolean;
  Formal: boolean;
  // Expand: boolean;
  // Shorten: boolean;
  Creative: boolean;
  Childify: boolean;
};

type MenuEntryType =
  | "Humanize"
  | "Formal"
  // | "Expand"
  // | "Shorten"
  | "Creative"
  | "Childify";
